import type { IActionDereference } from '@comunica/bus-dereference';
import type { IActionHttp } from '@comunica/bus-http';
import type { ICachePolicy, ICacheResponseHead, IRevalidationPolicy } from '@comunica/types';
/**
 * A wrapper around an HTTP request cache policy that exposes it as a dereference cache policy.
 */
export declare class DereferenceCachePolicyHttpWrapper implements ICachePolicy<IActionDereference> {
    private readonly cachePolicy;
    private readonly maxAcceptHeaderLength;
    constructor(cachePolicy: ICachePolicy<IActionHttp>, maxAcceptHeaderLength: number);
    storable(): boolean;
    satisfiesWithoutRevalidation(action: IActionDereference): Promise<boolean>;
    responseHeaders(): Headers;
    timeToLive(): number;
    revalidationHeaders(newAction: IActionDereference): Promise<Headers>;
    revalidatedPolicy(revalidationAction: IActionDereference, revalidationResponse: ICacheResponseHead): Promise<IRevalidationPolicy<IActionDereference>>;
}
