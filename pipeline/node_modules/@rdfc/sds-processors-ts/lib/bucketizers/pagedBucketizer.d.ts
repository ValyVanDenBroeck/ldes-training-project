import { AddRelation, Bucketizer, PageFragmentation, RemoveRelation } from "./index.js";
import { Bucket, RdfThing, Record } from "../utils/index.js";
import { BasicLensM, Cont } from "rdf-lens";
import { Term } from "@rdfjs/types";
export default class PagedBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    protected readonly pageSize: number;
    protected readonly path: BasicLensM<Cont, {
        value: string;
        literal?: Term;
    }>;
    protected readonly pathQuads: RdfThing;
    protected count: number;
    protected lastMemberTimestamp: number;
    constructor(config: PageFragmentation, save?: string);
    bucketize(record: Record, getBucket: (key: string, root?: boolean) => Bucket, addRelation: AddRelation, removeRelation: RemoveRelation): Bucket[];
    save(): string;
}
