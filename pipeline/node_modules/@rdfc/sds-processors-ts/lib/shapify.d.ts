import type { Stream, Writer } from "@rdfc/js-runner";
import { Quad, Term } from "@rdfjs/types";
export type Shape = {
    id: Term;
    quads: Quad[];
};
export declare function shapify(reader: Stream<string>, writer: Writer<string>, shape: Shape): Promise<void>;
