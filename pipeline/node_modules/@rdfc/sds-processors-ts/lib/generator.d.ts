import type { Writer } from "@rdfc/js-runner";
import { Term } from "@rdfjs/types";
export declare function generate(writer: Writer<string>, mCount?: number, mWait?: number, timestampPath?: Term): Promise<() => Promise<void>>;
