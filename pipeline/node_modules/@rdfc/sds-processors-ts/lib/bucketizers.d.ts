import { Quad, Term } from "@rdfjs/types";
import type { Stream, Writer } from "@rdfc/js-runner";
import { BucketizerConfig } from "./bucketizers/index.js";
import { Bucket, Record } from "./utils/index.js";
type Channels = {
    dataInput: Stream<string>;
    metadataInput: Stream<string>;
    dataOutput: Writer<string>;
    metadataOutput: Writer<string>;
};
type Config = {
    quads: {
        id: Term;
        quads: Quad[];
    };
    strategy: BucketizerConfig[];
};
export declare function record_to_quads(record: Record, resultingStream: Term, buckets: Bucket[]): Quad[];
export declare function bucketize(channels: Channels, config: Config, savePath: string | undefined, sourceStream: Term | undefined, resultingStream: Term, prefix?: string): Promise<void>;
export {};
