import { BasicLensM, Cont } from "rdf-lens";
import { AddRelation, Bucketizer } from "./index.js";
import { Bucket, Record } from "../utils/index.js";
export type Level = "year" | "month" | "day-of-month" | "hour" | "minute" | "second" | "millisecond";
export type TimeBucket = {
    ranges: Level[];
    amount: number;
};
export type TimeBucketTreeConfig = {
    path: BasicLensM<Cont, Cont>;
    pathQuads: Cont;
    levels: TimeBucket[];
    timeBufferMs: number;
};
export default class TimeBucketBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    private readonly config;
    private readonly path;
    private readonly state;
    constructor(config: TimeBucketTreeConfig, save?: string);
    bucketize(sdsMember: Record, getBucket: (key: string, root?: boolean) => Bucket, addRelation: AddRelation): Bucket[];
    save(): string;
}
