import { AddRelation, Bucketizer, HourFragmentation } from "./index.js";
import { Bucket, Record } from "../utils/index.js";
export default class HourBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    private readonly path;
    private readonly pathQuads;
    private readonly unorderedRelations;
    private hour;
    private root;
    constructor(config: HourFragmentation, save?: string);
    bucketize(record: Record, getBucket: (key: string, root?: boolean) => Bucket, addRelation: AddRelation): Bucket[];
    save(): string;
}
