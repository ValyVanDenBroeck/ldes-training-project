import { Bucketizer, DumpFragmentation } from "./index.js";
import { BasicLensM, Cont } from "rdf-lens";
import { Term } from "@rdfjs/types";
import { Bucket, RdfThing, Record } from "../utils/index.js";
export default class DumpBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    protected readonly path: BasicLensM<Cont, {
        value: string;
        literal?: Term;
    }>;
    protected readonly pathQuads: RdfThing;
    protected lastMemberTimestamp: number;
    constructor(config: DumpFragmentation, save?: string);
    bucketize(record: Record, getBucket: (key: string, root?: boolean) => Bucket): Bucket[];
    save(): string;
}
