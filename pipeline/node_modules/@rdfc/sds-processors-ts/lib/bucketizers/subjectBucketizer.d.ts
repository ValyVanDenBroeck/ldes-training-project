import { AddRelation, Bucketizer, SubjectFragmentation } from "./index.js";
import { Bucket, Record } from "../utils/index.js";
export default class SubjectBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    private readonly path;
    private readonly pathQuads;
    private readonly namePath?;
    private readonly defaultName?;
    private seen;
    constructor(config: SubjectFragmentation, save?: string);
    bucketize(record: Record, getBucket: (key: string, root?: boolean) => Bucket, addRelation: AddRelation): Bucket[];
    save(): string;
}
