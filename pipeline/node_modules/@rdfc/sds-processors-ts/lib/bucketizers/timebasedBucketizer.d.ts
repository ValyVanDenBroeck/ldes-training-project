import { AddRelation, Bucketizer, TimebasedFragmentation } from "./index.js";
import { Bucket, Record } from "../utils/index.js";
export default class TimebasedBucketizer implements Bucketizer {
    protected readonly logger: import("winston").Logger;
    private readonly path;
    private readonly pathQuads;
    private readonly maxSize;
    private readonly k;
    private readonly minBucketSpan;
    private mutableLeafBucketKeys;
    private members;
    constructor(config: TimebasedFragmentation, save?: string);
    bucketize(record: Record, getBucket: (key: string, root?: boolean, keyIsId?: boolean) => Bucket, addRelation: AddRelation): Bucket[];
    save(): string;
    private addTimestampRelations;
    private makeParentImmutableIfNoMutableChildren;
}
